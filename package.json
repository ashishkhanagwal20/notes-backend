{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build:ui": "rm -rf dist && cd ../part2 && npm run build && cp -r dist ../notes-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ashish Kumar",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "static": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
